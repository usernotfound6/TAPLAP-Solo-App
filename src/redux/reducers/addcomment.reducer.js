// const initialState = {
//     comments: [], // Initialize 'comments' as an empty array.
//   };

// const addcomment = (state = initialState, action) => {
//     switch (action.type) {
//       case "AD_COMMENT":
//         return {
//           ...state,
//           comments: [...state.comments, action.payload], // Add the new comment to the array.
//         };
//       default:
//         return state;
//     }
// }
// export default addcomment;